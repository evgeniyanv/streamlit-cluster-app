# =============================
# 🐍 Базовый образ
# =============================
FROM python:3.11-slim

# =============================
# 🏷️ Метаданные образа
# =============================
LABEL maintainer="Evgenia Zigaeva (Machine Learning Project, 2025)"
LABEL project="Steel Clustering Streamlit App"
LABEL description="Интерактивное Streamlit-приложение для кластеризации марок стали с визуализацией и анализом признаков."

# =============================
# ⚙️ Установка системных зависимостей
# =============================
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# =============================
# 📂 Рабочая директория внутри контейнера
# =============================
WORKDIR /app

# =============================
# 📦 Установка Python-зависимостей
# =============================
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# =============================
# 🧠 Копируем весь проект
# =============================
COPY . .

# =============================
# 🌐 Порт для доступа к приложению
# =============================
EXPOSE 8501

# =============================
# 🚀 Команда запуска приложения
# =============================
CMD ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
